<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>BC Confined Space Entry Permit Template | WorkSafeBC Part 9 | BC Safety Docs</title>
  <meta name="description" content="WorkSafeBC OHS Regulation Part 9 confined space entry permit template for BC contractors. BC-specific atmospheric thresholds, standby person requirements, and hazard classifications built in. Download instantly." />
  <meta name="keywords" content="BC confined space entry permit, WorkSafeBC Part 9 template, confined space permit BC, OHS Regulation Part 9 form, BC contractor safety forms, confined space entry permit Canada, WorkSafeBC confined space template" />
  <link rel="canonical" href="https://bcsafetydocs.com/" />

  <!-- Open Graph -->
  <meta property="og:title" content="BC Confined Space Entry Permit Template — WorkSafeBC Part 9" />
  <meta property="og:description" content="Built specifically for BC contractors. BC-specific hazard classifications, atmospheric thresholds, and standby person requirements — ready to fill out and use on site." />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="https://bcsafetydocs.com/" />

  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Barlow+Condensed:wght@600;700;800&family=Inter:wght@400;500;600&display=swap" rel="stylesheet" />

  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --black:   #1A1A1A;
      --yellow:  #F5C400;
      --white:   #FFFFFF;
      --grey:    #4A4A4A;
      --light:   #F4F4F4;
      --red:     #C0392B;
      --border:  #DDDDDD;
    }

    html { scroll-behavior: smooth; }

    body {
      font-family: 'Inter', sans-serif;
      font-size: 16px;
      color: var(--black);
      background: var(--white);
      line-height: 1.6;
      -webkit-font-smoothing: antialiased;
    }

    /* ── NAV ── */
    nav {
      position: fixed;
      top: 0; left: 0; right: 0;
      z-index: 100;
      background: var(--black);
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0 5%;
      height: 56px;
    }

    .nav-logo {
      font-family: 'Barlow Condensed', sans-serif;
      font-weight: 800;
      font-size: 1.2rem;
      color: var(--white);
      letter-spacing: 0.04em;
      text-decoration: none;
    }

    .nav-logo span { color: var(--yellow); }

    .nav-cta {
      font-family: 'Inter', sans-serif;
      font-size: 0.82rem;
      font-weight: 600;
      background: var(--yellow);
      color: var(--black);
      padding: 8px 18px;
      border-radius: 3px;
      text-decoration: none;
      letter-spacing: 0.02em;
      transition: opacity 0.15s;
    }

    .nav-cta:hover { opacity: 0.85; }

    /* ── HERO ── */
    .hero {
      margin-top: 56px;
      min-height: calc(100vh - 56px);
      display: grid;
      grid-template-columns: 1fr 1fr;
      position: relative;
      overflow: hidden;
    }

    .hero-left {
      background: var(--black);
      padding: 80px 6% 80px 8%;
      display: flex;
      flex-direction: column;
      justify-content: center;
      position: relative;
      z-index: 2;
    }

    /* Diagonal cut on right edge of left panel */
    .hero-left::after {
      content: '';
      position: absolute;
      top: 0; right: -60px;
      width: 120px;
      height: 100%;
      background: var(--black);
      clip-path: polygon(0 0, 50% 0, 100% 100%, 0 100%);
      z-index: 3;
    }

    .hero-right {
      background: var(--yellow);
      padding: 80px 8% 80px 10%;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .eyebrow {
      font-family: 'Barlow Condensed', sans-serif;
      font-size: 0.78rem;
      font-weight: 700;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: var(--yellow);
      margin-bottom: 16px;
    }

    .hero-headline {
      font-family: 'Barlow Condensed', sans-serif;
      font-weight: 800;
      font-size: clamp(2.6rem, 4.5vw, 3.8rem);
      line-height: 1.0;
      color: var(--white);
      letter-spacing: -0.01em;
      margin-bottom: 24px;
    }

    .hero-headline em {
      font-style: normal;
      color: var(--yellow);
    }

    .hero-sub {
      font-size: 1rem;
      color: #AAAAAA;
      line-height: 1.65;
      max-width: 420px;
      margin-bottom: 36px;
    }

    .hero-cta-group { display: flex; flex-direction: column; gap: 12px; max-width: 280px; }

    .btn-primary {
      display: block;
      text-align: center;
      background: var(--yellow);
      color: var(--black);
      font-family: 'Barlow Condensed', sans-serif;
      font-size: 1.1rem;
      font-weight: 700;
      letter-spacing: 0.06em;
      text-transform: uppercase;
      padding: 16px 28px;
      border-radius: 3px;
      text-decoration: none;
      transition: opacity 0.15s, transform 0.15s;
    }

    .btn-primary:hover { opacity: 0.9; transform: translateY(-1px); }

    .price-note {
      font-size: 0.82rem;
      color: #777;
      text-align: center;
    }

    /* Hero right — form preview card */
    .form-card {
      background: var(--white);
      border-radius: 4px;
      box-shadow: 0 8px 40px rgba(0,0,0,0.18);
      overflow: hidden;
      max-width: 420px;
    }

    .form-card-header {
      background: var(--red);
      padding: 14px 20px;
    }

    .form-card-header h3 {
      font-family: 'Barlow Condensed', sans-serif;
      font-weight: 700;
      font-size: 1.05rem;
      color: var(--white);
      letter-spacing: 0.04em;
    }

    .form-card-header p {
      font-size: 0.72rem;
      color: rgba(255,255,255,0.75);
      margin-top: 2px;
    }

    .form-card-body { padding: 20px; }

    .form-field {
      margin-bottom: 10px;
    }

    .form-label {
      display: block;
      background: #ECF0F1;
      font-size: 0.68rem;
      font-weight: 600;
      color: #555;
      padding: 4px 8px;
      letter-spacing: 0.04em;
    }

    .form-input {
      display: block;
      width: 100%;
      height: 28px;
      border: 1px solid #DDD;
      border-top: none;
    }

    .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 8px; }

    .hazard-row {
      display: flex;
      gap: 16px;
      margin: 12px 0 8px;
      font-size: 0.78rem;
      font-weight: 600;
    }

    .hazard-row span { display: flex; align-items: center; gap: 5px; }
    .hazard-row .cb { width: 12px; height: 12px; border: 1.5px solid #999; display: inline-block; }
    .hazard-row .cb.checked { background: var(--red); border-color: var(--red); }

    .form-tag {
      display: inline-block;
      background: #EBF5FB;
      border: 1px solid #AED6F1;
      font-size: 0.65rem;
      color: #1A5276;
      padding: 2px 8px;
      border-radius: 2px;
      margin-top: 6px;
    }

    /* ── TRUST BAR ── */
    .trust-bar {
      background: var(--light);
      border-top: 3px solid var(--yellow);
      padding: 20px 8%;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 48px;
      flex-wrap: wrap;
    }

    .trust-item {
      display: flex;
      align-items: center;
      gap: 10px;
      font-size: 0.85rem;
      font-weight: 600;
      color: var(--grey);
    }

    .trust-icon {
      width: 20px;
      height: 20px;
      background: var(--yellow);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 0.7rem;
      color: var(--black);
      font-weight: 800;
      flex-shrink: 0;
    }

    /* ── SECTIONS ── */
    section { padding: 80px 8%; }

    .section-label {
      font-family: 'Barlow Condensed', sans-serif;
      font-size: 0.75rem;
      font-weight: 700;
      letter-spacing: 0.16em;
      text-transform: uppercase;
      color: var(--red);
      margin-bottom: 12px;
    }

    .section-title {
      font-family: 'Barlow Condensed', sans-serif;
      font-weight: 800;
      font-size: clamp(1.8rem, 3vw, 2.6rem);
      line-height: 1.1;
      color: var(--black);
      margin-bottom: 16px;
    }

    .section-body {
      font-size: 1rem;
      color: var(--grey);
      max-width: 580px;
      line-height: 1.7;
    }

    /* ── PROBLEM SECTION ── */
    .problem { background: var(--black); }
    .problem .section-title { color: var(--white); }
    .problem .section-label { color: var(--yellow); }

    .problem-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 32px;
      margin-top: 48px;
    }

    .problem-card {
      border-top: 3px solid var(--yellow);
      padding-top: 20px;
    }

    .problem-card h4 {
      font-family: 'Barlow Condensed', sans-serif;
      font-weight: 700;
      font-size: 1.1rem;
      color: var(--white);
      margin-bottom: 8px;
      letter-spacing: 0.02em;
    }

    .problem-card p {
      font-size: 0.88rem;
      color: #888;
      line-height: 1.65;
    }

    /* ── WHAT'S INCLUDED ── */
    .includes { background: var(--white); }

    .includes-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 48px;
      margin-top: 48px;
      align-items: start;
    }

    .includes-list { list-style: none; }

    .includes-list li {
      display: flex;
      align-items: flex-start;
      gap: 12px;
      padding: 12px 0;
      border-bottom: 1px solid var(--border);
      font-size: 0.92rem;
      color: var(--grey);
      line-height: 1.5;
    }

    .includes-list li:last-child { border-bottom: none; }

    .check {
      width: 20px;
      height: 20px;
      background: var(--yellow);
      border-radius: 2px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 0.65rem;
      font-weight: 800;
      color: var(--black);
      flex-shrink: 0;
      margin-top: 1px;
    }

    .reg-card {
      background: var(--light);
      border-left: 4px solid var(--red);
      padding: 28px;
      border-radius: 2px;
    }

    .reg-card h4 {
      font-family: 'Barlow Condensed', sans-serif;
      font-weight: 700;
      font-size: 1.1rem;
      color: var(--black);
      margin-bottom: 16px;
      letter-spacing: 0.02em;
    }

    .reg-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 8px 0;
      border-bottom: 1px solid var(--border);
      font-size: 0.82rem;
    }

    .reg-row:last-child { border-bottom: none; }
    .reg-row .reg-label { color: var(--grey); }
    .reg-row .reg-val { font-weight: 600; color: var(--black); font-family: 'Barlow Condensed', sans-serif; font-size: 0.95rem; letter-spacing: 0.02em; }

    /* ── WHO IT'S FOR ── */
    .who { background: var(--light); }

    .who-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 20px;
      margin-top: 40px;
    }

    .who-card {
      background: var(--white);
      border: 1px solid var(--border);
      border-radius: 3px;
      padding: 24px;
    }

    .who-card h4 {
      font-family: 'Barlow Condensed', sans-serif;
      font-weight: 700;
      font-size: 1.05rem;
      color: var(--black);
      margin-bottom: 8px;
    }

    .who-card p {
      font-size: 0.85rem;
      color: var(--grey);
      line-height: 1.6;
    }

    /* ── CTA SECTION ── */
    .cta-section {
      background: var(--yellow);
      text-align: center;
      padding: 80px 8%;
    }

    .cta-section .section-title { color: var(--black); }
    .cta-section .section-body { color: var(--black); opacity: 0.7; margin: 0 auto 36px; }

    .price-block {
      display: inline-flex;
      align-items: baseline;
      gap: 6px;
      margin-bottom: 28px;
    }

    .price-amount {
      font-family: 'Barlow Condensed', sans-serif;
      font-weight: 800;
      font-size: 3.5rem;
      color: var(--black);
      line-height: 1;
    }

    .price-currency {
      font-family: 'Barlow Condensed', sans-serif;
      font-weight: 700;
      font-size: 1.4rem;
      color: var(--black);
    }

    .price-once {
      font-size: 0.85rem;
      color: rgba(0,0,0,0.55);
      margin-left: 4px;
    }

    .btn-dark {
      display: inline-block;
      background: var(--black);
      color: var(--white);
      font-family: 'Barlow Condensed', sans-serif;
      font-size: 1.15rem;
      font-weight: 700;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      padding: 18px 48px;
      border-radius: 3px;
      text-decoration: none;
      transition: opacity 0.15s, transform 0.15s;
      margin-bottom: 16px;
    }

    .btn-dark:hover { opacity: 0.85; transform: translateY(-1px); }

    .cta-disclaimer {
      font-size: 0.75rem;
      color: rgba(0,0,0,0.5);
      max-width: 480px;
      margin: 16px auto 0;
      line-height: 1.6;
    }

    /* ── FOOTER ── */
    footer {
      background: var(--black);
      padding: 32px 8%;
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: 16px;
    }

    .footer-logo {
      font-family: 'Barlow Condensed', sans-serif;
      font-weight: 800;
      font-size: 1rem;
      color: var(--white);
      letter-spacing: 0.04em;
    }

    .footer-logo span { color: var(--yellow); }

    footer p {
      font-size: 0.75rem;
      color: #555;
      line-height: 1.6;
      max-width: 480px;
    }

    footer a { color: #777; text-decoration: none; }
    footer a:hover { color: var(--yellow); }

    /* ── RESPONSIVE ── */
    @media (max-width: 900px) {
      .hero {
        grid-template-columns: 1fr;
        min-height: auto;
      }

      .hero-left::after { display: none; }

      .hero-left { padding: 60px 6%; }
      .hero-right {
        padding: 40px 6%;
        background: #F9F9F9;
      }

      .problem-grid { grid-template-columns: 1fr; gap: 24px; }
      .includes-grid { grid-template-columns: 1fr; }
      .who-grid { grid-template-columns: 1fr; }

      .trust-bar { gap: 24px; }
    }

    @media (max-width: 600px) {
      section { padding: 56px 5%; }
      .hero-left { padding: 48px 5%; }
      .hero-right { padding: 32px 5%; }
      .form-card { max-width: 100%; }
      footer { flex-direction: column; align-items: flex-start; }
      .trust-bar { padding: 20px 5%; }
    }

    @media (prefers-reduced-motion: reduce) {
      .btn-primary, .btn-dark { transition: none; }
    }
  </style>
</head>
<body>

  <!-- NAV -->
  <nav>
    <a href="/" class="nav-logo">BC<span>SAFETY</span>DOCS</a>
    <a href="#buy" class="nav-cta">Download — $67 CAD</a>
  </nav>

  <!-- HERO -->
  <section class="hero" aria-label="Hero">
    <div class="hero-left">
      <p class="eyebrow">WorkSafeBC OHS Regulation Part 9</p>
      <h1 class="hero-headline">The confined space<br>permit built for<br><em>BC contractors.</em></h1>
      <p class="hero-sub">Not a generic US template. Not a blank form. A two-page, regulation-specific confined space entry permit with BC atmospheric thresholds, standby person requirements, and hazard classifications already built in.</p>
      <div class="hero-cta-group">
        <a href="#buy" class="btn-primary">Download Template — $67 CAD</a>
        <p class="price-note">Word document (.docx) — instant download</p>
      </div>
    </div>

    <div class="hero-right" aria-hidden="true">
      <div class="form-card">
        <div class="form-card-header">
          <h3>CONFINED SPACE ENTRY PERMIT</h3>
          <p>WorkSafeBC OHS Regulation Part 9  ·  s.9.1, 9.25, 9.29–9.36</p>
        </div>
        <div class="form-card-body">
          <div class="form-row">
            <div class="form-field">
              <span class="form-label">Permit No.</span>
              <div class="form-input"></div>
            </div>
            <div class="form-field">
              <span class="form-label">Issue Date</span>
              <div class="form-input"></div>
            </div>
          </div>
          <div class="form-field">
            <span class="form-label">Confined Space Location / ID</span>
            <div class="form-input"></div>
          </div>
          <div class="form-field">
            <span class="form-label">2  HAZARD CLASSIFICATION  (s.9.1 / s.9.25)</span>
            <div class="hazard-row">
              <span><span class="cb"></span> LOW</span>
              <span><span class="cb"></span> MODERATE</span>
              <span><span class="cb checked"></span> HIGH</span>
            </div>
          </div>
          <div class="form-field">
            <span class="form-label">Atmospheric Testing  —  O2 / LEL / H2S / CO</span>
            <div class="form-input"></div>
          </div>
          <div class="form-field">
            <span class="form-label">Standby Person  (s.9.35 / s.9.36)</span>
            <div class="form-input"></div>
          </div>
          <span class="form-tag">✓ BC-specific thresholds</span>
          <span class="form-tag">✓ 2-page permit + monitoring log</span>
        </div>
      </div>
    </div>
  </section>

  <!-- TRUST BAR -->
  <div class="trust-bar" role="complementary" aria-label="Trust signals">
    <div class="trust-item"><div class="trust-icon">✓</div> BC OHS Regulation Part 9 specific</div>
    <div class="trust-item"><div class="trust-icon">✓</div> Sections 9.1, 9.25, 9.29–9.36 referenced</div>
    <div class="trust-item"><div class="trust-icon">✓</div> Word doc — fully editable</div>
    <div class="trust-item"><div class="trust-icon">✓</div> Instant download</div>
  </div>

  <!-- PROBLEM -->
  <section class="problem" aria-labelledby="problem-title">
    <p class="section-label">The problem</p>
    <h2 class="section-title" id="problem-title">Generic templates don't cut it<br>on a BC worksite.</h2>
    <div class="problem-grid">
      <div class="problem-card">
        <h4>US-based forms miss BC classification</h4>
        <p>BC uses a three-tier atmospheric hazard classification — Low, Moderate, High — with specific thresholds under s.9.1 and s.9.25. Most templates online are built for OSHA, not WorkSafeBC. They don't match what a BC officer is looking for.</p>
      </div>
      <div class="problem-card">
        <h4>Free WorkSafeBC samples are starting points, not forms</h4>
        <p>WorkSafeBC's own sample documents are intentionally generic. They don't have your atmospheric reading columns, standby person requirements per hazard level, or the amendment signature blocks you need for a real entry.</p>
      </div>
      <div class="problem-card">
        <h4>Enterprise safety software is overkill</h4>
        <p>Platforms like SafetyCulture and SafetyEvolution charge $25–$30 per user per month. For a crew of four doing one confined space entry a week, that's $100+/month for a permit you could have for $67 once.</p>
      </div>
    </div>
  </section>

  <!-- WHAT'S INCLUDED -->
  <section class="includes" aria-labelledby="includes-title">
    <p class="section-label">What's included</p>
    <h2 class="section-title" id="includes-title">Everything a BC supervisor needs.<br>Nothing they don't.</h2>
    <div class="includes-grid">
      <ul class="includes-list">
        <li><span class="check">✓</span> Two-page fillable Word document (.docx) — edit on any device, print on site</li>
        <li><span class="check">✓</span> BC atmospheric hazard classification (Low / Moderate / High) with threshold references per s.9.1 and s.9.25</li>
        <li><span class="check">✓</span> Atmospheric testing log: O2 ≥19.5%, LEL &lt;20%, H2S ≤10 ppm ceiling, CO &lt;25 ppm TWA</li>
        <li><span class="check">✓</span> Standby person requirements per hazard level built in — Low, Moderate, and High protocols per s.9.35 / s.9.36</li>
        <li><span class="check">✓</span> Supplied-air and SCBA requirements referenced per s.9.29</li>
        <li><span class="check">✓</span> Isolation, lockout, and purge verification checklist</li>
        <li><span class="check">✓</span> Full PPE checklist including supplied-air respirator, SCBA, and emergency escape respirator</li>
        <li><span class="check">✓</span> Rescue plan section — method, equipment, emergency contacts, nearest hospital</li>
        <li><span class="check">✓</span> 6-entry entrant sign-in/sign-out log plus 6-entry continuation on Page 2</li>
        <li><span class="check">✓</span> 16-row extended atmospheric monitoring log for long-duration entries</li>
        <li><span class="check">✓</span> Permit amendment signature blocks for mid-job condition changes</li>
        <li><span class="check">✓</span> Supervisor authorization and formal permit closure blocks</li>
        <li><span class="check">✓</span> Regulatory section references throughout (s.9.1, 9.25, 9.29–9.36)</li>
      </ul>

      <div class="reg-card">
        <h4>BC-Specific Atmospheric Thresholds</h4>
        <div class="reg-row"><span class="reg-label">O2 minimum</span><span class="reg-val">≥ 19.5%</span></div>
        <div class="reg-row"><span class="reg-label">LEL action threshold</span><span class="reg-val">&lt; 20% LEL</span></div>
        <div class="reg-row"><span class="reg-label">H2S ceiling</span><span class="reg-val">≤ 10 ppm</span></div>
        <div class="reg-row"><span class="reg-label">CO TLV-TWA</span><span class="reg-val">&lt; 25 ppm</span></div>
        <div class="reg-row"><span class="reg-label">Low hazard vent. minimum</span><span class="reg-val">85 m³/h per worker</span></div>
        <div class="reg-row"><span class="reg-label">Retest interval (vacated)</span><span class="reg-val">&gt; 20 min absent</span></div>
        <div class="reg-row"><span class="reg-label">Regulation</span><span class="reg-val">BC OHS Reg. Part 9</span></div>
        <div class="reg-row"><span class="reg-label">Key sections</span><span class="reg-val">s.9.1, 9.25, 9.29–9.36</span></div>
      </div>
    </div>
  </section>

  <!-- WHO IT'S FOR -->
  <section class="who" aria-labelledby="who-title">
    <p class="section-label">Who it's for</p>
    <h2 class="section-title" id="who-title">Built for the trades that<br>actually go underground.</h2>
    <div class="who-grid">
      <div class="who-card">
        <h4>Utility &amp; Pipeline Contractors</h4>
        <p>Valve chambers, meter pits, pipeline crossings. Any entry into an enclosed or partially enclosed space requires a permit under Part 9. Have one ready before the crew shows up.</p>
      </div>
      <div class="who-card">
        <h4>Plumbing &amp; Drainage</h4>
        <p>Manholes, sump pits, catch basins. WorkSafeBC inspectors know what a compliant BC permit looks like — make sure yours does too.</p>
      </div>
      <div class="who-card">
        <h4>Hydrovac &amp; Excavation</h4>
        <p>Excavations that qualify as confined spaces under Part 9 require the same controls. This permit covers the classification and atmospheric testing you need documented.</p>
      </div>
      <div class="who-card">
        <h4>Safety Officers &amp; Owner-Operators</h4>
        <p>If you're building a safety program for a small BC contractor without a dedicated safety coordinator, this is a professionally formatted, regulation-referenced starting point.</p>
      </div>
    </div>
  </section>

  <!-- BUY -->
  <section class="cta-section" id="buy" aria-labelledby="cta-title">
    <p class="section-label" style="color: rgba(0,0,0,0.5);">Get the template</p>
    <h2 class="section-title" id="cta-title">Ready before your next entry.</h2>
    <p class="section-body">Download once. Add your company name and logo. Print and use on site. No subscription, no login, no monthly fee.</p>
    <div class="price-block">
      <span class="price-currency">$</span>
      <span class="price-amount">67</span>
      <span class="price-currency">CAD</span>
      <span class="price-once">one-time</span>
    </div>
    <br>
    <a href="YOUR_LEMONSQUEEZY_OR_STRIPE_LINK_HERE" class="btn-dark">Download Now — Instant Access</a>
    <br>
    <p style="font-size: 0.82rem; color: rgba(0,0,0,0.5); margin-top: 8px;">Word document (.docx) · Instant download · No subscription</p>
    <p class="cta-disclaimer">This template is a professional working tool. All documents must be reviewed by a qualified person prior to use and verified against the current WorkSafeBC OHS Regulation. This product does not constitute legal or regulatory advice.</p>
  </section>

  <!-- FOOTER -->
  <footer>
    <div class="footer-logo">BC<span>SAFETY</span>DOCS</div>
    <p>WorkSafeBC OHS Regulation compliance document templates for BC contractors.<br>
    Questions: <a href="mailto:info@bcsafetydocs.com">info@bcsafetydocs.com</a></p>
    <p style="color: #444;">© 2026 BC Safety Docs. All rights reserved.<br>
    <a href="#">Terms</a> · <a href="#">Privacy</a></p>
  </footer>

</body>
</html>